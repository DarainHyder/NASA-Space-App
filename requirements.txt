flask==3.0.0
werkzeug==3.0.1
requests==2.31.0
pandas==2.1.1
numpy==1.26.0
# scikit-learn and xgboost are removed from Vercel to fit the 500MB limit.
# Inference will be handled via the Hugging Face API.